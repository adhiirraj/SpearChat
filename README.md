# SpearChat
End to End Encrypted Chat Application with CRYSTALS-Kyber(ML-KEM), AES-256-GCM, Per-message Nonces, 1-on-1 DMs and multi-user rooms, File / image / video transfer (chunked, encrypted), User accounts with session management, server holds no keys, no plaintext.
